name: ${pluginName}
version: ${version}
main: ${mainClass}
api-version: "${apiVersion}"
depend: [${dependsOn}]
softdepend: [${softDependsOn}]
author: MineDrop Network
description: Security foundation — plugin validation, handshake, and runtime integrity
